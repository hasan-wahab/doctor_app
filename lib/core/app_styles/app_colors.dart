import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  /// Primary Color
  static Color primaryColor = Color.fromRGBO(7, 169, 150, 1);

  /// Secondary Color
  static Color secondaryColor = Color.fromRGBO(231, 242, 241, 1);

  /// Background Color
  static Color bgColor = Colors.white;

  /// Text Black Color
  static Color firstTextBlackColor = Colors.black;

  ///  Secondary Text Color
  static Color secondaryTextColor = Color.fromRGBO(0, 0, 0, 0.5);

  /// Third Text Color
  static Color textWhiteColor = Colors.white;

  /// Black Icon Color
  static Color blackIconColor = Colors.black;

  /// White Icon Color
  static Color whiteIconColor = Colors.white;

  /// Link Text Color
  static Color linkTextColor = Color.fromRGBO(16, 70, 188, 1);
}
